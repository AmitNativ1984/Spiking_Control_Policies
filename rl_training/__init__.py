"""Training code for this repo, mirroring aerial_gym.rl_training.

Deliberately empty: importing this package must not pull in torch, because
`import isaacgym` has to happen first in any entry point (see rl_games/runner.py).
"""
